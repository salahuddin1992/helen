"""CommClient-Server operator tools package.

This package collects standalone operator-facing scripts (build helpers,
data cleanup, certificate authority utilities, etc.). Import as a normal
package so the scripts can share helpers without polluting the global
namespace.
"""
